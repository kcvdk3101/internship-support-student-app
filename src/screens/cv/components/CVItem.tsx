import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

const CVItem = (props: Props) => {
  return (
    <View>
      <Text>CVItem</Text>
    </View>
  )
}

export default CVItem

const styles = StyleSheet.create({})
